groups:
  data:
    - postgres
    - redis

variables:
  RAILS_ENV: development

services:
  rails:
    repo: https://github.com/dosuken123/devkitkat-example-rails.git
    host: localhost
    port: 3000
  postgres:
    host: localhost
    port: 5432
  redis:
    host: localhost
    port: 6379
